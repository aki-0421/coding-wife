{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-runtime",
  "description": "Read-only runtime metadata for the single main window.",
  "windows": ["main"],
  "permissions": []
}
