# Development-only reference: the core app requires no environment variables.
# Coding sessions reuse the authenticated local Codex installation.
# Optional TTS is configured in App Settings and stored by the native app.
# Never place an OpenAI API key or another secret in this file.
