---
title: "画面詳細仕様テンプレート"
description: "Tauri + Next.js デスクトップアプリの個別画面仕様を作成するための雛形。"
updated: 2026-07-16
read_when:
  - "新しい `S-NNN` 画面詳細仕様を作成するとき。"
  - "個別画面で決めるべき状態、操作、ネイティブ連携を確認するとき。"
---
# 画面詳細仕様テンプレート

## 使い方

1. `agent-docs list docs/screen-design` で既存の画面詳細仕様を確認する。
2. [ID管理ルール](id-management-rules.md#画面id)に従い、未使用の画面IDを決める。
3. 「テンプレート本体」以下をコピーする。
4. `docs/screen-design/S-NNN_<screen-name>.md` として保存する。
5. front matterを含む `<...>` をすべて実値へ置き換える。決められない内容は「未確定事項」へ移す。
6. [デスクトップ共通仕様](../screen-design/desktop-common-specification.md)と重複する内容は書かず、この画面固有の動作または差分だけを書く。
7. 関連する要件定義書と要件IDの相互参照を確認する。
8. `agent-docs lint` を実行する。

> テンプレート本体の `desktop-common-specification.md` リンクは、コピー先の `docs/screen-design/S-NNN_<screen-name>.md` を基準にしている。このテンプレート原本上では解決しないが、指定の配置へコピーすると解決するため書き換えない。

---

## テンプレート本体

---
title: "S-NNN <画面名>"
description: "<この画面の目的を1文で記載する。>"
updated: <YYYY-MM-DD>
read_when:
  - "<この画面の仕様、状態、操作、関連要件を確認するとき。>"
screen_id: "S-NNN"
status: "Draft"
---
# S-NNN \<画面名\>

| 項目 | 内容 |
|---|---|
| window label | `<label>` |
| Next.js path / view key | `<pathまたはview key>` |
| 対象OS | `<macOS / Windows / Linux / 共通>` |
| デザイン | `<参照先 / 未作成>` |
| 共通仕様 | [デスクトップ共通仕様](desktop-common-specification.md) |
| 廃止理由 | `<理由 / 非該当>` |
| 後継画面ID | `<S-NNN / 非該当>` |

## 目的

> この画面でユーザーが達成することを1〜3行で書く。レイアウトや実装方法ではなく、ユーザー目的を記載する。

\<ユーザー目的\>

## 対象範囲

### 含める

| 対象 | 内容 |
|---|---|
| `<対象>` | `<この画面で扱うこと>` |

### 含めない

| 非対象 | 理由 | 扱う画面・文書 |
|---|---|---|
| `<非対象>` | `<理由>` | `<参照先>` |

## 表示契機と終了

| 項目 | 内容 |
|---|---|
| 表示契機 | `<アプリ起動 / 画面操作 / メニュー / tray / deep link / ファイル関連付け等>` |
| 表示前提 | `<必要な状態・データ・権限>` |
| 初期フォーカス | `<対象要素>` |
| 正常完了 | `<保存、遷移、ウィンドウ動作>` |
| キャンセル | `<状態維持、遷移、ウィンドウ動作>` |
| 閉じる操作 | `<破棄 / 非表示 / 確認 / アプリ終了>` |
| 再表示 | `<状態復元 / 初期化>` |

## 利用者と権限

| 利用者・ロール | 表示 | 操作 | 拒否時の動作 |
|---|---|---|---|
| `<利用者>` | `<可 / 不可 / 条件付き>` | `<許可する操作>` | `<表示・遷移>` |

## 画面構成

| 領域 | 表示内容 | 主な操作 |
|---|---|---|
| `<領域名>` | `<情報>` | `<操作>` |

## 表示状態

同じユーザー目的の読み込み中、空状態、エラー等は別画面IDにせず、この表で管理する。

| 状態 | 進入条件 | 表示 | 操作可否 | 状態から抜ける条件 |
|---|---|---|---|---|
| 初期化中 | `<条件>` | `<表示>` | `<可否>` | `<条件>` |
| 通常 | `<条件>` | `<表示>` | `<可否>` | `<条件>` |
| データなし | `<条件>` | `<表示>` | `<可否>` | `<条件>` |
| 処理中 | `<条件>` | `<表示>` | `<可否>` | `<条件>` |
| オフライン | `<条件>` | `<表示>` | `<可否>` | `<条件>` |
| エラー | `<条件>` | `<表示>` | `<可否>` | `<条件>` |
| 権限不足 | `<条件>` | `<表示>` | `<可否>` | `<条件>` |

不要な状態は削除せず、「非該当」と理由を記載する。

## 操作

| 操作 | 事前条件 | 正常結果 | キャンセル時 | 失敗時 | 関連要件ID |
|---|---|---|---|---|---|
| `<操作>` | `<条件>` | `<観測可能な結果>` | `<状態>` | `<表示・再試行・データ保持>` | `<PREFIX-F-NNN>` |

## 入力項目

入力がない画面では、この節を「非該当」とする。

| 項目 | 初期値 | 必須 | 制約・境界 | エラー表示 | 保存契機 |
|---|---|---|---|---|---|
| `<項目>` | `<値>` | `<必須 / 任意 / 条件付き>` | `<制約>` | `<表示>` | `<契機>` |

## ネイティブ連携

実際のCapability設定は `src-tauri/capabilities/` を正本とする。この表には、画面が必要とする操作とユーザー向けの失敗契約を書く。

| ユーザー操作 | 実行境界 | Tauri plugin / Command | 必要なCapability・認可 | キャンセル時 | 拒否・失敗時 |
|---|---|---|---|---|---|
| `<操作>` | `<WebView / Tauri plugin / Rust Command / 外部API>` | `<名称 / 未定>` | `<permission / Rust側認可 / 非該当>` | `<動作>` | `<表示・状態維持>` |

画面IDやNext.js path / view keyをCapability境界として扱わない。同じwindow label内で画面ごとに異なる権限が必要なら、Rust側の認可条件またはウィンドウ分離を記載する。

## ウィンドウ固有動作

[デスクトップ共通仕様](desktop-common-specification.md)との差分だけを書く。差分がなければ「共通仕様どおり」とする。

| 項目 | 動作 |
|---|---|
| 生成・再利用 | `<動作>` |
| 初期サイズ・最小サイズ | `<値>` |
| リサイズ | `<可否と再配置>` |
| 最大化・全画面 | `<可否>` |
| 常に手前へ表示 | `<可否>` |
| 閉じる操作 | `<動作>` |
| 未保存変更がある場合 | `<確認・保存・破棄>` |

## メニュー・ショートカット

| 操作 | macOS | Windows / Linux | 有効条件 | 実行結果 |
|---|---|---|---|---|
| `<操作>` | `<キー>` | `<キー>` | `<条件>` | `<結果>` |

該当しない場合は「非該当」とする。OS予約キーや一般的な操作慣習との競合を確認する。

## データ保持

| データ | 正本・保存先 | 保存契機 | 復元契機 | 破棄条件 | 失敗時 |
|---|---|---|---|---|---|
| `<データ>` | `<保存先>` | `<契機>` | `<契機>` | `<条件>` | `<動作>` |

## OS差分

差分がない項目は「共通」とする。未確認を「共通」とみなさない。

| 項目 | macOS | Windows | Linux |
|---|---|---|---|
| `<項目>` | `<動作>` | `<動作>` | `<動作>` |

## アクセシビリティ

- キーボードだけで主要操作を完了できる。
- フォーカス順と初期フォーカスが一意である。
- エラーや状態を色だけで伝えない。
- 操作要素にアクセシブルな名前がある。
- OSの文字サイズ、コントラスト、動きの抑制設定を不必要に阻害しない。

画面固有の追加要件があれば記載する。

## 関連要件

| 要件ID | この画面での扱い | 要件定義書 |
|---|---|---|
| `<PREFIX-F-NNN>` | `<新規 / 変更 / 参照>` | `<リンク>` |

## 未確定事項

| 論点 | 初期判断 | 確認事項 | 着手ブロック |
|---|---|---|---|
| `<決まっていないこと>` | `<暫定方針 / なし>` | `<誰に何を確認するか>` | `<はい / いいえ / 不明>` |

## レビュー確認

- [ ] front matterの `screen_id`、タイトル、ファイル名の画面IDが一致している。
- [ ] `status` が `Draft`、`Approved`、`Deprecated` のいずれかである。
- [ ] 目的と対象外が一意である。
- [ ] 初期化、通常、空、処理中、オフライン、エラー、権限不足を確認した。
- [ ] キャンセル、閉じる、再表示、未保存データの動作が決まっている。
- [ ] ネイティブ操作のCapability・認可と失敗時動作が決まっている。
- [ ] OS差分を確認し、未確認を「共通」としていない。
- [ ] 関連要件IDが要件定義書と一致している。
- [ ] 着手ブロックが「はい」または「不明」の未確定事項が残っていない。
- [ ] `agent-docs lint` が成功している。
